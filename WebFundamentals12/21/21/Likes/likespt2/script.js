


function votea(){
    var element = document.querySelector("#likea")
    var newText = parseInt(element.innerText) + 1;
    element.innerText = newText + " like(s)";
}
function voteb(){
    var element = document.querySelector("#likeb")
    var newText = parseInt(element.innerText) + 1;
    element.innerText = newText + " like(s)";
}
function votec(){
    var element = document.querySelector("#likec")
    var newText = parseInt(element.innerText) + 1;
    element.innerText = newText + " like(s)";
}